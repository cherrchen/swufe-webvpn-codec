# Known Issues: Phase 1 本机桥（001-phase1-local-bridge）

> Spec ID: 001
> Status: In Progress
> Owner: cherrchen
> Last Updated: 2026-09-23

> 本文件是 T039 要求的**已知问题登记表**：每个已知问题一行，按 `KI-1xx` 编号单调递增、不复用。
> 规则：`severity` ∈ `P0` / `P1` / `P2`；`status` ∈ `Open` / `Fixed` / `Accepted` / `Deferred`。
> 「已记录」不等于「已修复」：`Deferred` 与 `Accepted` 必须在 `note` 里写清原因与解除条件，且同步到
> [verification.md](verification.md) 的「未验证 / 无法验证项」。`Fixed` 条目必须写明修复点与复验方式。

## 登记表

| id | title | severity | status | linked_case | owner | note |
| -- | ----- | -------- | ------ | ----------- | ----- | ---- |
| KI-001 | Windows 真机项未验证（TC-G03 及 Windows 侧 C02..C04/E01/E02/G04） | P0 | Fixed | TC-G03、TC-C02..C04、TC-E01/E02、TC-G04 | cherrchen | **已修复（2026-09-23，Windows 11 24H2 真机）**：Windows 侧项按 [development-run.md](../../docs/operations/development-run.md) 与本文件 M4 手册全部执行——TC-G03（教务经桥可打开并可操作）、TC-C01..C04、TC-E01/E02、TC-D01..D04、TC-F01/F04、TC-G04、TC-H01/H02/B05 通过；同时暴露并修复 4 个 Windows 专属缺陷（`KI-015`..`KI-018`）与 4 处工具平台假设（`KI-020`）。原延期原因=机器不在本轮可访问环境，现机器可用（Node v24.14.0 + `uv sync` + 真实账号）。执行与证据见 verification.md 的「M4 Windows 验收执行记录」 |
| KI-002 | 进程捕获真实范围未验证（只有所选应用经桥） | P1 | Fixed | TC-G04 | cherrchen | M4 实机通过（2026-09-21，CDP 驱动 + 手工）：切到「指定应用」后系统代理为 `Enabled: No`（互斥），`localCaptureEnabled=true`；被选中的 Google Chrome 请求出现在桥日志（`jwxt.swufe.edu.cn rewritten=true` + `response body`），未选中的 `curl` 直连 `jwxt.swufe.edu.cn` 失败且桥日志无对应行。本机未出现扩展授权提示（扩展此前已授权），授权引导文案的复验见 NFR-005 的 M3 记录 |
| KI-003 | 日志面板与真实桥的端到端联动未验证 | P1 | Fixed | TC-F04 | cherrchen | M4 实机通过：真实桥流量下 `#log-rows` 累计到 200 条上限且最新在前，行内容只有「时间 / 域名 / 结果」（例：`jwxt.swufe.edu.cn` `响应改写（body）`），关闭开关后面板隐藏且行数归 0，再打开可见且为 0 行 |
| KI-004 | `kill -TERM` / `SIGKILL` 不触发 JS 清理，系统代理残留到下次启动 | P2 | Accepted | NFR-004 | cherrchen | 已由 `recoverOnLaunch()` 自愈并实测；第一期接受该行为（进程被强杀时没有可执行的清理时机）。M4 另实测：`SIGTERM`（hub restart/stop）会走 `before-quit` 清理并清空代理 |
| KI-005 | Windows 不广播 `WM_SETTINGCHANGE`，已运行浏览器可能需重启才感知代理 | P2 | Accepted | TC-C02（Windows） | cherrchen | 实现已知限制（`apps/desktop/src/main/platform/win32/system-proxy.ts` 头注释）；随 KI-001 在真机复验 |
| KI-006 | 会话 Cookie 名与另外两个失效信号未在实机确认（Q-001） | P2 | Open | TC-D01/TC-D03 | cherrchen | M4 已采集（只记名不记值）：`wengine_vpn_ticketwebvpn_swufe_edu_cn`、`route`、`show_vpn`、`heartbeat`、`show_faq`（域 `.webvpn.swufe.edu.cn` / `webvpn.swufe.edu.cn`，共 5 条）；已确认的失效信号 = 探测 `GET https://webvpn.swufe.edu.cn/` 得 `302 → /login`（日志原文：`会话探测：status=302 location=https://webvpn.swufe.edu.cn/login → expired`）。另两个信号（Set-Cookie 清空、连续改写后 302 到 CAS）本轮未观测到；Q-001 收敛前保持 `Open`。**2026-09-21 决策（cherrchen）**：本轮**不**补实现另两个信号，也暂不以现有信号正式收敛 Q-001——保持 `Open`，待有真实会话环境时再观测/决策（TC-D03 现以探测信号通过） |
| KI-007 | 自动安装 CA 失败：提权子进程无法弹出信任设置授权（须手动执行应用给出的命令） | P1 | Fixed | TC-E01 | cherrchen | 实测（macOS 15.6）：osascript `with administrator privileges` 能写入钥匙串，但随后 `SecTrustSettingsSetTrustSettings: The authorization was denied since no user interaction was possible. (1)`，界面显示「已安装，但系统尚未信任」并给出 `sudo security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain <caCert>`。TC-E01 以该手动命令通过（`verify-cert` 成功、`getCaStatus() = {installed:true,trusted:true}`）。建议修复方向：改为由应用进程直接调用 `security`（让 SecurityAgent 在本 App 的 GUI 会话内弹授权，而不是在 osascript 子进程内），属安全模型变更，需 ADR。解除条件：按上述方向实现并在真机复验。**已修复（2026-09-21，[ADR-0008](../../docs/architecture/adr/ADR-0008-ca-trust-authorization-in-app-session.md)）**：根因确认 = 写信任设置的进程必须处于用户 GUI 会话内（osascript 提权子进程由 securityd 在会话外派生），钥匙串写入则相反必须为 root；另一条实测把根因钉死——同样从提权子进程执行 `security trust-settings-import -d <plist>` 也报 `SecTrustSettingsImportExternalRepresentation: The authorization was denied since no user interaction was possible. (1)`。修复点 = `apps/desktop/src/main/platform/darwin/cert.ts` 的 `install()` 拆成两步：① 提权 `security add-certificates -k /Library/Keychains/System.keychain <caCert>`（只写钥匙串、不碰信任设置；重复导入的 `already in …` 视为成功）；② 由**应用进程自己**执行 `security add-trusted-cert -d -r trustRoot <caCert>`（不带 `-k`，因此不改动钥匙串，只写管理域信任设置）；`exec.ts` 的 `runPrivilegedDarwin()` 适用范围收窄为「仅写钥匙串」并改用 120s 授权超时。复验方式（应用内一次点击，macOS 15.6）：系统钥匙串含该证书（`find-certificate -c mitmproxy -Z`）、`security trust-settings-export -d` 出现该 CA 的 SHA-1、`security verify-cert -c <caCert> -p ssl` 退出码 0、以该 CA 签发的叶证书经 `/usr/bin/curl`（SecureTransport）访问返回 200、`getCaStatus() = {installed:true,trusted:true}`、`#ca-state` = 「已安装并被系统信任」、界面文案不含 `sudo`；卸载回归 TC-E02 见同节。完整记录见 [verification.md](verification.md) 的「`KI-007` 修复复验执行记录（2026-09-21）」 |
| KI-008 | 会话探测未携带登录分区 Cookie，真实会话被误判过期（桥启动后数十秒即被停） | P0 | Fixed | TC-D02/TC-D03、AC-008 | cherrchen | 根因：Electron `net.request` 默认 `useSessionCookies: false`，探测请求不带 `persist:swufe-login` 的 Cookie → 门户一律返回 `302 → /login` → `SESSION_EXPIRED` 级联（停桥、清代理、清会话、弹窗）。修复：`apps/desktop/src/main/session-broker.ts` 的 `probe()` 显式 `useSessionCookies: true`。复验：同一 Cookie 修前 302、修后 200（隔离实验见 verification.md 的 M4 记录）；真机上桥连续跨越 ≥5 个探测周期（8s）保持 `running`。影响面：修复前每次登录后 ≤30s（默认探测间隔）就会自毁，属首用即失败级别 |
| KI-009 | 登录窗初始加载被跨站重定向中断（ERR_ABORTED）被判致命：误导性报错 + 页面停在只有扫码入口的初始渲染 | P1 | Fixed | TC-D01 | cherrchen | 现象：点「登录 WebVPN」后界面显示「打开登录窗口失败：ERR_ABORTED (-3) loading 'authserver…'」，而 CAS 页停在服务端初始 HTML（`tabHead` 仍为 `display:none`，只有微信扫码，无账号/密码入口）→ 用户无法用账号登录。修复：`openLogin()` 只把「窗口已关闭」与 `ERR_ABORTED` 视为非致命（`isAbortError`），真实错误照旧抛出。复验：修复后登录窗自动显示「账号登录/验证码登录 + 用户名/密码」（`#username` 可见、`tabHead` 为 `block`），`#message` 不再出现报错 |
| KI-010 | 卸载 CA 无法取得证书指纹（`security find-certificate` 缺 `-Z`） | P1 | Fixed | TC-E02 | cherrchen | 根因：macOS 15.6 下不带 `-Z` 时 `security find-certificate -a -c mitmproxy /Library/Keychains/System.keychain` 只打印属性 dump，没有 `SHA-1 hash:` 行 → `parseFindCertificate().sha1` 为 `null` → 界面报「卸载失败：未能取得系统信任库中的证书指纹。」且永远无法卸载。修复：`apps/desktop/src/main/platform/darwin/cert.ts` 的选择器加 `-Z`（并补测：带 `-Z` 的输出形态与不带 `-Z` 的形态各一例，均为实测）。复验：卸载成功（钥匙串 `wc -c` = 0、UI 显示「未安装」、`getCaStatus() = {installed:false}`）。残留观察：卸载后 `security verify-cert` 仍返回成功（信任设置项残留），但钥匙串已无该证书，UI 判定以钥匙串为准 |
| KI-011 | 网关客户端 shim 与透明桥不兼容：教务首页白屏、页面不可交互（阻塞 TC-G01/G02） | P0 | Fixed | TC-G01、TC-G02、AC-007 | cherrchen | 实测：网关对每个 HTML 页面注入 `__vpn_*` 变量与 `<script src="/wengine-vpn/js/main.js?ver=…">`（`__vpn_protocol_host=https://webvpn.swufe.edu.cn`），该 shim 期望浏览器工作在 WebVPN URL 空间。透明桥把相对路径 `/wengine-vpn/js/main.js` 加上 token 前缀 → 经桥 **404**（网关根直取为 **200 / 376922B**）→ shim 永不启动。结果：`http://jwxt.swufe.edu.cn/` 白屏（925B shim 页）；`/xtgl/index_initMenu.html` 经桥返回 200/76854B 且体已被反向改写，但页面脚本依赖 shim → 渲染出来但不可交互（cherrchen 实测：未登录、用户状态不正确、任何控件点击无效、未出现 CAS 登录页）。**已修复（2026-09-21，M5，[ADR-0007](../../docs/architecture/adr/ADR-0007-gateway-owned-namespaces-and-native-mode-promotion.md)）**：修复点 = `bridges/python/swufe_bridge/addon.py` 的 `GATEWAY_ROOT_PREFIXES`（`/wengine-vpn/`、`/authserver/` 直通取自网关根、其响应不反向改写）+ `_promote_to_gateway`（改写过的 `GET`/`HEAD` 响应命中判据时以 `302` 升级到该 URL 的 WRD 形态），判据 = `bridges/python/swufe_bridge/rewrite.py` 的 `is_gateway_bootstrap_html`（`text/html` + ≤ `GATEWAY_BOOTSTRAP_MAX_BYTES` = 8192 + 同时含 `GATEWAY_BOOTSTRAP_MARKERS`；实测引导页 925 B、真实页 76854 B，量级差 >80×，判据可分）。复验 = L1 `test_gateway_owned_path_is_not_token_wrapped`、`test_gateway_owned_authserver_path_is_not_token_wrapped`、`test_site_path_containing_gateway_prefix_is_still_wrapped`、`test_gateway_owned_path_on_non_allowlisted_host_is_untouched`、`test_gateway_owned_response_is_not_reverse_rewritten`、`test_bootstrap_document_is_promoted`、`test_site_page_with_injected_shim_is_not_promoted` + L2 `test_gateway_owned_path_and_promotion_end_to_end` + macOS 实机（入口 `http://jwxt.swufe.edu.cn/` → `detail=promoted` → 网关原生空间首页与站内「学生成绩查询」均可交互；`/wengine-vpn/js/main.js` 经桥 200 / 376922B；`www.swufe.edu.cn` 仍为普通主机名）→ TC-G01/TC-G02 通过（详见 [verification.md](verification.md) 的 M5 记录） |
| KI-012 | 个别直连主机经 mitmproxy 无响应（实测 `www.swufe.edu.cn`） | P2 | Open | — | cherrchen | 实测：`curl http://www.swufe.edu.cn/` 直连 200（0.22s），经桥 0 字节超时；同一台机器上**不带 addon** 的裸 mitmdump 同样复现（`server connect www.swufe.edu.cn:80 (125.69.85.81:80)` 后无响应），而 `example.com` / `baidu.com` / `google.com` 经桥正常。故与桥代码无关，疑为上游/WAF 行为；不在验收路径上（教务经网关、登录走 authserver） |
| KI-013 | 本机 Clash/mihomo TUN + fake-ip DNS 与桥共存会导致上游挂起，且冲突检测不覆盖 TUN | P1 | Fixed | 验收环境前置条件 | cherrchen | 实测：TUN 开启时上游域名被解析到 `198.18.0.0/15`（Clash fake-ip），经桥请求大面积挂起（含教务）；cherrchen 关闭 TUN/系统代理后 `webvpn.swufe.edu.cn` 等路径立即恢复。`PROXY_CONFLICT` 只检测系统代理，不检测 TUN/虚拟网卡 → 建议：开桥前的环境预检或错误文案补充「请关闭 VPN/代理工具的 TUN 模式」，并把该前置条件写入 [development-run.md](../../docs/operations/development-run.md)。**已修复（2026-09-21，[ADR-0011](../../docs/architecture/adr/ADR-0011-refuse-start-on-fake-ip-dns.md)）**：修复点 = 新增 `apps/desktop/src/main/fake-ip.ts` 的 `isFakeIpAddress`（判据 `198.18.0.0/15`）与 `ProxyOrchestrator.runStart()` 的 fake-ip 预检（位于系统代理冲突检查之后、端口探测之前：解析 `settings.webvpnBase` 的主机，命中即以**既有**错误码 `PROXY_CONFLICT` 拒绝启动；解析失败按 fail open 跳过，不阻断）。用户文案改为同时覆盖两种成因（[main-window.md](../../docs/ui-ux/main-window.md) 的代理冲突提示 + 冲突模态标题/正文）。复验：`apps/desktop/test/fake-ip.test.ts`（判据边界）+ `apps/desktop/test/orchestrator.test.ts`（命中拒绝且未触碰 OS、预检顺序在端口探测之前、解析失败不阻断；共 76 passed）；真机 smoke（真实 DNS `webvpn.swufe.edu.cn → 202.115.115.140` 时正常开桥；注入 fake-ip 解析则拒绝且 OS 调用只剩 `proxy.read`、无 sidecar）。残留：该预检只覆盖 fake-ip 形态，`redir-host` 模式的 TUN 仍可能挂起 → 前置条件保留（[development-run.md](../../docs/operations/development-run.md) §1） |

## 验收期新增

本轮 M4 验收（macOS 交互式 + Windows 延期）与本轮 M5 复验（`KI-011` 修复）新发现的问题按 `KI-1xx` 追加到上表，并在 [verification.md](verification.md) 的映射表与「执行的命令与结果」中对齐。

| KI-014 | CAS 登录页主题静态资源在传输中间歇截断（`ERR_INCOMPLETE_CHUNKED_ENCODING`）导致登录窗渲染错乱 | P1 | Accepted | TC-D01 | cherrchen | 现象（M5 复验期间由 cherrchen 报告「登录页面出现渲染错误」）：登录窗的 CAS 多因子认证页样式丢失、弹层文字重叠。定位（执行者，CDP）：`bootstrap.min.css` 在 `document.styleSheets` 中但 `cssRules.length = 0`（未禁用）；同页 `Log.entryAdded` 与 `Network.loadingFailed` 均为 `net::ERR_INCOMPLETE_CHUNKED_ENCODING`（`bootstrap.min.css`、`jquery-latest.min.js`）；重载后 `cssRules = 1187`、`decodedBodySize = 121048`（= 服务端完整长度），布局恢复正常。独立复现（不涉 Electron / 桥 / 系统代理）：`curl --http2 'https://authserver.swufe.edu.cn/authserver/swufeThemezxqr/static/css/bootstrap.min.css?v=…'` 11 次中 3 次被截断（`33612` / `25136` / `26880` B，伴 `curl: (18) transfer closed with … bytes remaining to read`），完整值为 `121048` B。故障在不经 Electron/桥的直连请求上复现，表明问题位于外部传输路径；现有证据不能区分校方服务端与中间网络设备。Chromium 丢弃未完整加载的 CSS/JS；与 [ADR-0007](../../docs/architecture/adr/ADR-0007-gateway-owned-namespaces-and-native-mode-promotion.md) 的改动无关（登录分区为 `DIRECT`，且当时桥未启用，INV-004）。规避：重载登录窗（或关闭后重新点「登录 WebVPN」）。解除条件：外部传输恢复稳定；若发现本地组件可复现同一截断则重开；产品侧「重载」引导若另立任务需单独定范围。**2026-09-21 决策（cherrchen）**：**不**做产品侧改动（不新增「重载」引导），仅保留文档化规避——已写入 [development-run.md](../../docs/operations/development-run.md) §6 常见故障；本条目当时保持 `Open`。**2026-09-23 复核与决策（cherrchen）**：先前 macOS 直连 11 次中 3 次截断已独立排除应用与桥；本轮 Windows 对同资源在 TUN 开启时以真实 IP、fake-ip 及物理网卡源地址各测 6 次，18 次均完整（当前未复现，不等于外部故障永久消失）。按本轮「非本地原因则推进验证」的条件接受外部传输风险及重载规避，置 `Accepted`；若本地组件复现同一截断则重开 |

### Windows 真机验收新增（2026-09-23）

Windows 侧 M4 执行（见 [verification.md](verification.md) 的「M4 Windows 验收执行记录」）新发现的问题。`KI-015`..`KI-018` 四个产品缺陷都是 Windows **首次真机运行**才暴露的，且都落在验收路径上（登录、CA、系统代理）。

| id | title | severity | status | linked_case | owner | note |
| -- | ----- | -------- | ------ | ----------- | ----- | ---- |
| KI-015 | 登录成功后残留「尚未登录」错误（状态机 `error` 不被新会话清除） | P1 | Fixed | TC-D01 | cherrchen | 现象（cherrchen 报告「登录后仍然显示错误」）：`startBridge()` 以 `NOT_LOGGED_IN` 失败后状态机停在 `error`，而登录**不**重置它 → 状态条按 `error` 优先显示「错误」+ 旧文案「尚未登录 WebVPN…」，直到下一次开桥/关桥才消失；此时 `getSession().loggedIn` 已为 `true`。根因：`BridgeStateMachine.reset()` 只由 start/stop 调用，登录路径没有对应处理（macOS 侧 M4 的顺序恰好没有暴露）。修复：`ProxyOrchestrator.clearSessionExpiredNotice()` 更名 `clearStaleSessionNotice()` 并覆盖 `NOT_LOGGED_IN`（会话已建立 ⇒ 成因消失），调用点仍在 `ipc.ts` 的 `session.onChange`。复验：`apps/desktop/test/orchestrator.test.ts` 新增两例（登录后清除陈旧 `NOT_LOGGED_IN`；非会话成因的 `CA_MISSING` 不被清除）+ Windows 真机（登录后 `state=idle`、`loggedIn=true`、状态条「已登录」） |
| KI-016 | 中文 Windows 下 certutil 的本地化标签与失效选择器导致 CA 状态误判（导入成功却报「查不到该证书」） | P0 | Fixed | TC-E01、TC-E02、AC-005 | cherrchen | 实测（Windows 11 24H2，中文界面）：① `certutil -user -store Root <id>` **忽略** `<id>` 且非空存储时恒退出 0——真实指纹 / 全零指纹 / 证书名三种入参输出逐字节相同（md5 `f4f3f32d7250fc3322e90612b3b0b5ec`），旧实现的 `result.code === 0` 判据无效；② 哈希标签被本地化为 `证书哈希(sha1):`，旧 `parseCertutilHashes` 只认英文 `Cert Hash(sha1):` → `installed` 恒为 `false` → 点「安装本机 CA」实际导入成功仍报「证书已导入，但当前用户根存储中仍查不到该证书」，随后 `startBridge()` 以 `CA_MISSING` 拒绝（TC-E01 在 Windows 上必失败）。修复：`apps/desktop/src/main/platform/win32/cert.ts` 改为读用户根存储的注册表后备 `HKCU\Software\Microsoft\SystemCertificates\Root\Certificates\<指纹>`（键存在 ⇔ 已安装，与语言无关且按指纹精确），删除语言相关的 `parseCertutilHashes` 及其单测；卸载仍用 `certutil -user -delstore Root <指纹>`（实测按指纹精确删除：以一次性诱饵证书验证同存储内其它证书不受影响）。复验：真机安装 2.7s 成功、UI「已安装并被系统信任」、注册表键 = 证书指纹；`apps/desktop/test/cert-manager.test.ts` 改为按「存储存在性 + 指纹身份」断言，不再依赖命令输出文本 |
| KI-017 | `execFile` 的 stdin 管道使 `certutil` 挂起至超时（Windows CA 安装必失败） | P0 | Fixed | TC-E01、TC-E02 | cherrchen | 实测：`certutil -user -addstore Root <pem>` 与 `certutil -user -store Root <无匹配>` 都会读到 stdin 才退出；而 `child_process.execFile` 的 `stdio` 选项被忽略（实测 `{stdio:['ignore','pipe','pipe']}` 下 `node -e 'process.stdin.resume()'` 仍被超时杀掉）。应用侧现象 =「安装失败：命令超时（10000ms）：certutil」。对照实验（同一命令、同一台机、同一时刻）：stdin 关闭 `0.038s`；stdin 为保持打开的管道 `15.005s`（= 管道关闭时刻）。修复：`apps/desktop/src/main/exec.ts` 的 `run()` 改用 `spawn(command, args, { stdio: ['ignore','pipe','pipe'] })`，自行实现超时（超时仍 reject 为「命令超时（Nms）：<cmd>」）与 8 MiB 输出上限，spawn 失败仍保持「不抛、返回退出码 1」的契约；新增 `apps/desktop/test/exec.test.ts`（排水 stdin 的命令必须完成、非零退出码不抛、stdout/stderr 采集）。复验：Windows 真机 CA 安装/卸载成功；`reg` / `certutil` / `tasklist` / python CA 生成全部经新 `run()` 正常 |
| KI-018 | `reg query` 的 CRLF 输出使 WinINET 代理解析恒为空 → 漏报 `PROXY_CONFLICT` 且关桥不清代理 | P0 | Fixed | TC-C01、TC-C02、TC-C03、TC-C04、AC-003、AC-004 | cherrchen | 根因：`parseWinInetValue` 用 `(.*)$` 收尾的逐行正则，而 `.` 不匹配 `\r`，`reg query` 在 Windows 输出 CRLF → 每行都匹配失败 → `Win32SystemProxy.read()` 恒返回 `{enabled:false, server:null}`。后果（真机实测）：① 已有外部系统代理（`ProxyEnable=0x1`、`ProxyServer=127.0.0.1:7890`）时开桥**未**被 `PROXY_CONFLICT` 拒绝，直接进入 `running` 并把 `ProxyServer` 覆盖为 `127.0.0.1:8080`（违反 [ADR-0004](../../docs/architecture/adr/ADR-0004-refuse-start-when-system-proxy-in-use.md)）；② 关桥/退出后不清理本 App 设置的代理（`ProxyEnable` 仍为 `0x1`），却把「由本 App 设置」标记清掉，界面同时显示「系统代理：未由本 App 设置」——用户被留在指向已停端口的系统代理上，且 `recoverOnLaunch()` 因标记已清而无法自愈（违反 NFR-004）；③ 状态行读数不可信。旧单测只用 LF 样本，故从未暴露。修复：`parseWinInetValue` 逐行 `trim()` 后再匹配；`apps/desktop/test/system-proxy-parse.test.ts` 新增真机 CRLF 样本回归例。复验：真机 `read()` 返回 `{enabled:true, server:'127.0.0.1', port:8080}`；开桥前以 `PROXY_CONFLICT` + 冲突模态拒绝且注册表未被改动；关桥与退出应用后 `ProxyEnable=0x0`、无 sidecar 残留 |
| KI-019 | 经桥访问教务的请求偶发挂起数十秒（无字节返回） | P1 | Open | TC-G03、TC-F01 | cherrchen | 实测（Windows，TUN 已关，`SWUFE_PROBE_INTERVAL_MS=8000`）：经桥 `GET http://jwxt.swufe.edu.cn/` 在部分轮次 12–30s 无任何字节（`curl` 退出码 28、`size=0`），随后同目标若干轮 0.24–0.32s 正常；直连网关根（不经桥）按 20s 间隔连续 6 次均 0.20–0.95s 正常；挂起也出现在**刚重启 sidecar 的全新连接**上，且把 mitmproxy `http2_ping_keepalive` 调到 10s 后重测未见复现（样本仅 3 轮，证据不足）→ 排除「仅由旧连接复用导致」，仍无法区分本机传输层、mitmproxy 与网关；试验性改动已回滚。影响：浏览器首次打开教务页可能「卡住」，重载/关闭重开即恢复（cherrchen 实测「重新打开后正常」）；macOS 验收期有同类现象（「自动化导航在本环境反复卡死」）。**2026-09-23 原决策（cherrchen）：接受**——当时按疑似上游风险接受，已有可执行规避（重载）且不产生误导性结论（同轮同目标的后续请求均正常）；复测手段随本条记录（6 次自动化 smoke、20s 间隔对照、直连对照、keepalive 试验）。**同日复核**：使用重新登录的真实会话，对同一教务 WRD 路径执行 14 次固定真实 IP / Wi-Fi 源地址桥请求、22 次默认路由桥请求，以及同路径直连、20s 间隔与并发门户探测对照，均在约 0.2–0.5s 返回；当前未复现历史挂起。现用默认路由仍指向 Meta Tunnel，绑定源地址也不能证明完全绕过 TUN；旧对照只请求网关根，且请求日志位于上游连接之前，均不足以证明「非本地」。因此撤销基于该归因的 `Accepted`，重新置 `Open`；解除条件：在挂起当轮采集 `server_connect` / `server_connected` / `responseheaders` / `error` 事件及同一 WRD 路径直连结果，区分本机与外部路径，修复本地原因或经证据确认外部原因后再决策。**2026-09-23 改造与复测（执行者，本机）**：桥改为**有界失败 + 一次自动重试**并对上游各阶段留证——`bridges/python/swufe_bridge/upstream.py` 把网关主机的上游建连限制为 4s × 2 次尝试（`CONNECT_TIMEOUT_SECONDS` / `CONNECT_ATTEMPTS`，sidecar 在 `mitmdump()` 前安装），超时经 mitmproxy 既有失败路径转成客户端的 `502 Bad Gateway`；新增 `swufe-upstream` 记录（六键、十个阶段、`ms` 语义与触发条件见 [bridge-control-protocol.md](../../docs/api/bridge-control-protocol.md)）与 `<userData>/bridge-upstream.log`（JSONL，256 KiB 上限、脱敏：不含 URL/token/Cookie/正文），`swufe-ready` 增加 `upstream_connect_timeout_ms` / `upstream_connect_attempts` / `upstream_log` 三键。实测（不经校园网，本机）：① 不可达上游（RFC 5737 `192.0.2.1`）下经桥 `curl` **8.1s 内**返回 `HTTP/1.1 502 Bad Gateway`（`exit 0`，不再空等），阶段记录为 `connect_timeout`(4013ms) → `connect_retry` → `connect_timeout`(4005ms) → `connect_failed` → `error`，均含对端 `192.0.2.1:80`（L2 `bridges/python/tests/l2/test_upstream_stall.py`、`pnpm run acceptance:check` 现报 `FAIL bridge-smoke 上游 502（KI-019 有界失败）`）；② 慢响应（4.5s）记录 `response` 且 `ms >= 3000`；③ 已连但不回字节的上游：客户端 `curl -m 6` 退出码 28，桥侧留下 `connect_start` → `connect_done` → `tls_failed` → `error`（`detail=connection closed`）——即「已连上但不响应」**无法**被中断（mitmproxy `flow.kill()` 不作用于在途 flow），只能看见，此为本条已知边界。现场复测**未执行**：本机无应用会话（`%APPDATA%\swufe-webvpn-bridge` 不存在、无 Electron/sidecar 进程、系统代理 `ProxyEnable=0`），按「不可诊断的情形不得据此下结论」的口径**保持 `Open`**。解除条件更新为：在与历史失败轮同形的环境（默认路由直连、无 TUN 路由）下执行 `pnpm run diagnose:upstream --user-data-dir <profile> --rounds 40`，取得至少 1 个非 `no-stall` 轮及其阶段记录与同轮直连对照后按判据决策——出现 `network-or-resolver` 轮 → 可置 `Accepted`（解除条件：网络环境变更后复测）；只有 `bridge-side` 轮且阶段记录指向 `connect_timeout` / `connect_failed` → 保持 `Open` 并记录「客户端不再无限等待」的实测；`bridge-side` 轮且阶段记录指向 `tls_*` 或缺 `response` → 单行试验 `sidecar.py` 的 argv 增加 `--set http2=false` 后再跑一轮 |
| KI-020 | 测试与证据工具的平台假设在 Windows 上误报或静默跳过 | P2 | Fixed | TC-A..TC-H（L0/L1/L2）、`pnpm run acceptance:check` | cherrchen | 首次 Windows 回归暴露 4 类非产品缺陷，全部已修并复验：① 0600 权限断言在 Windows 合成模式位（`0o666`）下恒失败（`tests/l0/test_config.py`、`tests/l1/test_ca.py`、`apps/desktop/test/orchestrator.test.ts`）→ 改为仅 POSIX 断言，Windows 的等价保护（`%APPDATA%` 每用户 ACL）写入注释与 [development-run.md](../../docs/operations/development-run.md)；② L2 用例与 `scripts/acceptance-collect.ts` 用 Windows curl（Schannel）校验自签 MITM 证书时报 `curl: (60) schannel: the revocation status is unknown` → win32 追加 `--ssl-no-revoke`；③ L2 断言依赖 HTTP/2 小写头名（Windows curl 无 h2，走 HTTP/1.1 保留原大小写）→ 头名折叠大小写比较；④ 回环约束用例的地址发现依赖 `ifconfig`（Windows 不存在）→ 该安全用例被静默跳过 → 改为 `getaddrinfo` + 路由探测，Windows 上真正执行并通过；另：`apps/desktop/test/cert-parse.test.ts` 的路径断言假设 POSIX 分隔符（改为平台无关断言）、证据脚本的权限位检查在 win32 误报 `FAIL`（改为 `INFO` + 说明）。复验：Windows `uv run --directory bridges/python pytest -q` = `198 passed`（此前 `196 passed / 1 skipped / 3 failed`）、`pnpm --filter swufe-webvpn-bridge run test:unit` = `108 passed`、`pnpm run acceptance:check` 无 `FAIL` |
