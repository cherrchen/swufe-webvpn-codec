# Tasks: Phase 1 本机桥（001-phase1-local-bridge）

> Spec ID: 001
> Status: In Progress
> Owner: cherrchen
> Last Updated: 2026-09-21

> 本文件供 Coding Agent **逐条执行**。任务必须足够小、可独立验证、说明输入输出与依赖。
> **禁止**把「实现整个 Feature」当作一个任务。
> 状态：`- [ ]` 未完成，`- [x]` 已完成。完成后立即更新，不要批量事后补勾。

## Task 格式

```markdown
- [ ] T001 <动作，动词开头> — 输入：… — 输出：… — 依赖：无 — 验证：<命令或检查> — 关联：REQ-001 / AC-001
```

要求：

| 字段 | 说明 |
| ---- | ---- |
| ID | `T` + 三位序号，单调递增，不复用 |
| 动作 | 一个具体改动（改哪个文件/模块） |
| 输入 | 需要的前置信息或产物 |
| 输出 | 完成后仓库中的可观察结果（文件、行为） |
| 依赖 | 前置任务 ID，无则写 `无` |
| 验证 | 可执行的验证方式（命令、检查或手工步骤） |
| 关联 | 对应的需求/验收标准 ID |

> 阶段对应关系：Phase 1 → WBS 1（MS0）、Phase 2 → WBS 2（MS1）、Phase 3 → WBS 3（MS1）、Phase 4 → WBS 4（MS2–MS3）、Phase 5 → WBS 5（MS2）、Phase 6 → WBS 6（MS2–MS4）、Phase 7 → WBS 7（MS4/M5 候选）。里程碑退出条件见 [plan.md](plan.md)。

## Phase 1 — 文档与治理（WBS 1）

- [ ] T001 建立并注册 Phase 1 Spec 五个文件 — 输入：归档原包 `docs/archive/2026-09-20-swufe-webvpn-bridge-docs-v1.0/`、事实清单 — 输出：`specs/001-phase1-local-bridge/{spec,design,plan,tasks,verification}.md` 可被引用，`docs/planning/roadmap.md` Spec 索引含 001 行 — 依赖：无 — 验证：`pnpm run spec:check` 无 error；链接检查通过 — 关联：REQ-001..REQ-011 / NFR-001..NFR-007
- [ ] T002 补齐开源许可与安全说明（WBS 1.2，后置，M5 候选） — 输入：[design.md](design.md) §Security Considerations、[docs/security/README.md](../../docs/security/README.md)、G-004 / NFR-003 — 输出：`LICENSE`（MIT）与安全/风险声明（HTTPS 解密范围、CA 可卸载、仅服务有权使用 WebVPN 的用户） — 依赖：T001 — 验证：检查许可文件与风险声明存在且链接可达 — 关联：G-004 / NFR-003

- [x] T048 建立 ADR-0007 并同步网关自有命名空间与升级规则的相关文档 — 输入：T045/T046 的结论与实现、T047 的复验结果 — 输出：[ADR-0007](../../docs/architecture/adr/ADR-0007-gateway-owned-namespaces-and-native-mode-promotion.md)（中英）+ [ADR 索引](../../docs/architecture/adr/README.md)（中英）；[design.md](design.md)（Proposed Solution 第 4 点的例外条款、Components 表的 Bridge Addon 行、Architecture Impact 的 ADR 行、流程图说明与数据流）、[spec.md](spec.md)（REQ-007 与 AC-001/AC-007、Status → `Implemented`）、[known-issues.md](known-issues.md)（`KI-011` → `Fixed`、新增 `KI-014`）、[verification.md](verification.md)（映射表 + M5 记录 + 未验证项与结论）、[components.md](../../docs/architecture/components.md) / [interfaces.md](../../docs/architecture/interfaces.md) / [data-flow.md](../../docs/architecture/data-flow.md)（Bridge Addon 的关键不变式与既有承诺补两条例外）、[testing-strategy.md](../../docs/development/testing-strategy.md)（L1/L2 用例数与基线）与 [M4 里程碑](../../docs/planning/milestones/M4-acceptance.md) / [milestones/README](../../docs/planning/milestones/README.md) / [roadmap](../../docs/planning/roadmap.md)（TC-G01/G02 与 macOS 出口条件） — 输出文档与英文版成对更新 — 依赖：T046、T047 — 验证：`pnpm run docs:check` 0 error / 0 warning；`pnpm --filter swufe-webvpn-bridge run typecheck` 无 error — 关联：REQ-007 / AC-007 / [ADR-0007](../../docs/architecture/adr/ADR-0007-gateway-owned-namespaces-and-native-mode-promotion.md)

## Phase 2 — WRD 与核心库（WBS 2）

- [x] T003 固化 WRD codec 库（Python 权威实现） — 输入：归档原型 `99-appendix/wrd_codec.py`、[docs/api/wrd-codec-library.md](../../docs/api/wrd-codec-library.md) — 输出：可导入的 codec 模块，实现 `encryptHost` / `decryptHost` / `encodeUrl` / `decodeUrl`，默认 `key = iv = wrdvpnisthebest!` 且可配置覆盖 — 依赖：无 — 验证：`python -c` 对 authserver 样本 decode 得 `authserver.swufe.edu.cn`（TC-A01）、重编码一致（TC-A02） — 关联：REQ-006 / NFR-002 / ADR-0005
- [x] T004 建立 codec 向量测试 — 输入：T003 模块、归档样本（authserver / jwxt / 带端口） — 输出：L0 测试文件，覆盖样本 URL 解密、重编码一致、jwxt 编码可回解、`http-8080` scheme token、错误 key 失败 — 依赖：T003 — 验证：运行 L0 测试，TC-A01..TC-A05 全部通过 — 关联：REQ-006 / NFR-002 / AC-007
- [x] T005 实现 allowlist 匹配库 — 输入：[docs/architecture/data-model.md](../../docs/architecture/data-model.md) 的匹配算法 — 输出：`match(host)` 函数：精确命中、`includeSwufeWildcard` 时匹配 apex `swufe.edu.cn` 与 `.swufe.edu.cn` 后缀、`webvpn.swufe.edu.cn`/`authserver.swufe.edu.cn` 硬编码排除 — 依赖：无 — 验证：L0 测试 TC-B02、TC-B03、TC-B04 通过 — 关联：REQ-005 / REQ-008 / EC-001 / EC-003
- [x] T006 实现 allowlist 持久化与默认值 — 输入：T005、`AllowlistConfig` 实体定义 — 输出：`userData/config.json` 读写（`hosts`、`includeSwufeWildcard`、`updatedAt`；默认 `{"hosts":["jwxt.swufe.edu.cn"],"includeSwufeWildcard":false}`）与主机名小写化/合法性校验 — 依赖：T005 — 验证：L1 测试 TC-B01（默认含 jwxt）、TC-B05（增删后重启仍在） — 关联：REQ-005 / AC-006

## Phase 3 — Bridge Sidecar（WBS 3）

- [x] T007 搭建 mitm 工程骨架 — 输入：[ADR-0002](../../docs/architecture/adr/ADR-0002-reuse-mitmproxy-for-tls.md)、[docs/api/bridge-control-protocol.md](../../docs/api/bridge-control-protocol.md) — 输出：可启动的 mitmdump sidecar 入口（监听本地端口、专用 confdir、addon 加载点），开发模式使用本机 Python venv — 依赖：无 — 验证：启动 sidecar 后可经 `127.0.0.1:<bridge_port>` 代理发起一次 HTTPS 请求 — 关联：REQ-003 / NFR-001
- [x] T008 实现请求改写 addon — 输入：T003、T005、T007 — 输出：addon 识别 `scheme/host/port/path/query`；命中 allowlist 时生成 WebVPN URL、上游改为 `webvpn.swufe.edu.cn`、附加 WebVPN Cookie、按需最小必要调整 `Host`/`Origin`/`Referer`；未命中保持直连 — 依赖：T003、T005、T007 — 验证：L2 集成测试 TC-F01（curl 经代理访问 allowlist 主机，上游见 WebVPN 形态）、TC-F02（非 allowlist 直连且 `rewritten=false`） — 关联：REQ-006 / REQ-008 / EC-003 / EC-004
- [x] T009 实现响应反向改写：`Location` — 输入：T008 — 输出：响应 `Location` 由 WebVPN 形态回写为普通主机名语义 — 依赖：T008 — 验证：L1 组件测试用假上游返回 WebVPN Location，客户端跟随落到普通主机名（TC-F03） — 关联：REQ-007 / R-001
- [x] T010 实现响应反向改写：`Set-Cookie` 的 Domain/Path — 输入：T008 — 输出：与主机名相关的 Cookie 域/路径被改写，避免写到错误域 — 依赖：T008 — 验证：L1 组件测试断言改写后的 Cookie 属性落到真实主机域 — 关联：REQ-007 / R-001
- [x] T011 实现响应反向改写：HTML/JS/JSON 绝对 URL — 输入：T008 — 输出：`text/html`、`application/javascript`、`application/json` 中的校内绝对 URL 改写；其它内容类型不改写 — 依赖：T008 — 验证：L1 组件测试（录制流量/假上游）断言绝对 URL 被改写，非目标内容类型原样透传 — 关联：REQ-007 / R-001
- [x] T012 实现 Cookie/配置热更新通道 — 输入：T006、T008、[docs/api/bridge-control-protocol.md](../../docs/api/bridge-control-protocol.md) — 输出：运行中可推送/重载 `{allowlist, cookies, debug}`（含 `wrdKey`/`wrdIv` 覆盖），且 Cookie 不写入日志、不出现在控制口响应 — 依赖：T006、T008 — 验证：L1 测试：改 allowlist 后无需重启生效；检查日志与控制口响应中无 Cookie — 关联：REQ-002 / REQ-005 / NFR-003 / ADR-0005 — M1 证据：`bridges/python/tests/l1/test_addon_reload.py`；控制面为方案 A（配置文件 mtime 轮询），无控制口响应可查（见 [bridge-control-protocol.md](../../docs/api/bridge-control-protocol.md)）
- [x] T013 实现控制/健康检查口（可选） — 输入：T007、[docs/api/bridge-control-protocol.md](../../docs/api/bridge-control-protocol.md) — 输出：仅本机 `127.0.0.1` 的 `GET /health`、`POST /config`、`POST /shutdown`（若未采用该方式则实现等价的热加载通路） — 依赖：T007、T012 — 验证：curl 调 `/health` 返回存活；`/shutdown` 优雅退出 — 关联：REQ-003 / NFR-004 — M1 证据：未采用控制口，按任务允许的「等价通路」实现：配置文件热加载 + `swufe-ready` / `swufe-error` 进程信号面 + 结束进程即优雅停止，端到端断言见 `bridges/python/tests/l2/test_proxy_end_to_end.py`

- [x] T045 诊断取证网关自有命名空间与 bootstrap 页（`KI-011`） — 输入：M4 实测记录（`KI-011`、verification.md）、本机可匿名取到的网关根资源 — 输出：判定常数与证据——`/wengine-vpn/` 与 `/authserver/` 为网关自有根命名空间（未登录也可从网关根取到 `/wengine-vpn/js/js/wechat-font.js` 200）；bootstrap 页 = 网关注入 shim 的最小引导页（实测 925 B，含 `__vpn_` 与 `/wengine-vpn/js/main.js`），真实页 76854 B（量级差 >80×）；故 `GATEWAY_BOOTSTRAP_MAX_BYTES = 8192` 且判据可分（无需备用谓词） — 依赖：无 — 验证：`curl -sS -o /dev/null -w '%{http_code} %{size_download}\n' https://webvpn.swufe.edu.cn/wengine-vpn/js/main.js?ver=20211207` → `200 376922`；判据可分性由 M4 的 925 B / 76854 B 实测对照支撑 — 关联：REQ-007 / `KI-011`
- [x] T046 实现网关自有命名空间直通与 bootstrap 文档升级 — 输入：T045 的常数与判据 — 输出：`bridges/python/swufe_bridge/addon.py` 的 `GATEWAY_ROOT_PREFIXES`（命中前缀的上行改为 `{webvpnBase}{path}`，只注入 Cookie、不做 token 编码，其响应不反向改写）与 `_promote_to_gateway`（改写过的 `GET`/`HEAD` 响应命中判据 ⇒ `302` + `Cache-Control: no-store` 到该 URL 的 WRD 形态）；`bridges/python/swufe_bridge/rewrite.py` 的 `GATEWAY_BOOTSTRAP_MARKERS` / `GATEWAY_BOOTSTRAP_MAX_BYTES` / `is_gateway_bootstrap_html`（纯函数）；L1 7 例 + L2 1 例 — 依赖：T045、T008..T011 — 验证：`uv run --directory bridges/python pytest -q` = `198 passed`（新增 8 例：`bridges/python/tests/l1/test_addon_request.py` 4 例 + `bridges/python/tests/l1/test_addon_response.py` 3 例 + `bridges/python/tests/l2/test_proxy_end_to_end.py::test_gateway_owned_path_and_promotion_end_to_end`）；反向验证：撤掉直通与升级后新增用例失败 5 例（`GATEWAY_ROOT_PREFIXES=()` + `_promote_to_gateway` 直接 `return False`） — 关联：REQ-006 / REQ-007 / AC-007 / [ADR-0007](../../docs/architecture/adr/ADR-0007-gateway-owned-namespaces-and-native-mode-promotion.md)

## Phase 4 — Electron 应用（WBS 4）

- [x] T014 建立 Electron 工程脚手架 — 输入：[ADR-0003](../../docs/architecture/adr/ADR-0003-electron-gui-for-phase-1.md)、[docs/api/electron-ipc.md](../../docs/api/electron-ipc.md) — 输出：可启动的 Main/Preload/Renderer 结构与应用窗口，开发模式可运行 — 依赖：无 — 验证：启动应用显示主窗口（TC-H01 前置） — 关联：REQ-001 / AC-001 — M2 证据：`apps/desktop/` 包（Main/Preload/Renderer/shared + 三个 tsconfig），`pnpm start` 可用；单实例锁实测（第二个实例立即退出，首个保持运行）
- [x] T015 注册 preload 契约与 IPC 通道 — 输入：T014、[docs/api/electron-ipc.md](../../docs/api/electron-ipc.md) — 输出：`window.swufeBridge` 暴露全部方法名与 `BridgeStatus`/`DebugLogEvent` 类型声明，通道注册可被 Renderer 调用 — 依赖：T014 — 验证：Renderer 调用一次 `getStatus` 返回 `idle` — 关联：REQ-001 — M2 证据：`apps/desktop/src/main/ipc.ts` 注册全部 15 个方法，另加 `getSettings` / `onStatus` / `onSessionExpired`（见 [electron-ipc.md](../../docs/api/electron-ipc.md) 变更记录）；`apps/desktop/test/orchestrator.test.ts` 与实机 UI 覆盖
- [x] T016 实现登录 WebView 与 login/logout/getSession — 输入：T015 — 输出：内嵌窗口打开官方 WebVPN/CAS 供用户完成认证（含 MFA）；`login/logout/getSession` 返回 `{loggedIn, expiresAt?}` — 依赖：T015 — 验证：L3/手工完成 CAS 后 `loggedIn=true`（TC-D01）；未登录时 `startBridge` 返回 `NOT_LOGGED_IN`（TC-D02） — 关联：REQ-002 / AC-002 — M2 证据：`apps/desktop/src/main/session-broker.ts`；实机走通「打开门户 → 采集 Cookie → 已登录」，未登录时开桥开关禁用且 `startBridge` 返回 `NOT_LOGGED_IN`；真实 CAS 会话待 L3
- [x] T017 实现 Session Broker 的 Cookie 提取与持久化 — 输入：T016、`SessionState` 实体定义 — 输出：从登录 session partition 导出或按白名单拷贝 Cookie，写入 `userData/session.bin`（加密）或持久分区，权限收紧，不保存学号/密码 — 依赖：T016 — 验证：登录后会话文件存在且不含密码；Cookie 不会出现在日志（TC-D01、TC-F04） — 关联：REQ-002 / NFR-003 / AC-002 / R-002 — M2 证据：会话存于 Electron 持久分区 `persist:swufe-login`（不写 `session.bin`，data-model 允许该形态）；不存学号/密码；Cookie 只以 `name/value/domain/path` 写入 `bridge-config.json`（0600）
- [x] T018 实现防环 — 输入：T016 — 输出：登录 WebView 走 direct（或 bypass 含 `webvpn.swufe.edu.cn`、`authserver.swufe.edu.cn`）；已是 WebVPN 形态的请求直通不二次包装 — 依赖：T016、T008 — 验证：抓包/日志显示登录流量无 WRD 二次包装（TC-D04）；访问 webvpn 主机时 `rewritten=false` — 关联：REQ-008 / EC-004 / EC-005 / AC-010 — M2 证据：登录分区 `setProxy({mode:'direct'})`；实机日志 `resolveProxy(...) = DIRECT`，登录期间无 `swufe-debug` 行；addon 侧硬编码排除由 M1 的 L1 用例覆盖
- [x] T019 实现会话失效检测与过期处理流程 — 输入：T017、T020、T026 — 输出：组合失效信号（探测 URL 返回登录页标记 / Set-Cookie 清空会话 / 连续改写后 302 到 CAS）触发 `SESSION_EXPIRED`：停桥 → 清系统代理 → 停进程捕获 → 弹窗重登 — 依赖：T017、T020 — 验证：模拟失效信号后桥停、系统代理清除、弹窗出现（TC-D03） — 关联：REQ-002 / EC-007 / AC-008 / R-002 — M2 证据：`apps/desktop/src/main/session-probe.ts` 分类 + `orchestrator.handleSessionExpired()`；实机在 302 → `/login` 后 8 秒内完成「清代理 → 停 sidecar → 清会话 → 弹窗」。Q-001 的另两个信号留 M3/M4
- [x] T020 实现 Proxy Orchestrator：冲突检测与系统代理 — 输入：T007、T029/T032（平台适配） — 输出：开桥前读 OS 代理——已启用且非本桥 ⇒ `PROXY_CONFLICT` 拒绝启动；否则设置 `127.0.0.1:<bridge_port>` 并记录「由本 App 设置」标记；关闭/退出仅在该标记存在时清除 — 依赖：T007 — 验证：TC-C01（已有代理拒绝启动且 OS 设置未变）、TC-C02（开桥设代理）、TC-C03（关桥清代理）、TC-C04（退出清代理） — 关联：REQ-003 / REQ-004 / EC-008 / AC-003 / AC-004 / NFR-004 / ADR-0004 — M2 证据：`networksetup` 读到 `127.0.0.1:7890` 已启用时拒绝启动且未改动 OS；开桥后 6 个启用服务全部指向 `127.0.0.1:8080`；关桥/退出清空（含 `recoverOnLaunch()` 自愈）
- [x] T021 实现 Proxy Orchestrator：sidecar 生命周期 — 输入：T007、T020 — 输出：spawn/stop mitmdump、桥状态机 `idle → starting → running`／`running → stopping → idle`／`starting → error → idle`，sidecar 异常退出报 `BRIDGE_CRASH` 且不留半开状态 — 依赖：T020 — 验证：开桥后状态为 `running`；杀 sidecar 后状态为 `error`（`BRIDGE_CRASH`），系统代理被清除 — 关联：REQ-003 / NFR-004 — M2 证据：`apps/desktop/src/main/sidecar.ts` + `state-machine.ts`；实机就绪判据（`swufe-ready` 行 + TCP 可连）、SIGTERM 停止、异常退出 → `BRIDGE_CRASH` 且清代理（单测 + 实机）
- [x] T022 实现 Cert Manager：CA 生成 — 输入：T007、[ADR-0002](../../docs/architecture/adr/ADR-0002-reuse-mitmproxy-for-tls.md) — 输出：使用 mitmproxy 专用 confdir 生成本机 MITM CA（不自研 PKI），提供 `getCaStatus` — 依赖：T007 — 验证：首次运行生成 CA 文件且私钥仅本机；`getCaStatus.installed=false`（TC-E03 前置） — 关联：REQ-010 / NFR-001 / NFR-003 — M2 证据：`bridges/python/swufe_bridge/ca.py`（`bridges/python/tests/l1/test_ca.py` 5 例）+ `apps/desktop/src/main/platform/ca-files.ts`；实机开桥时由 mitmdump 在 `<userData>/mitmproxy/` 生成，私钥 0600，`getCaStatus` 之前不隐式生成
- [x] T023 实现 Cert Manager：安装到系统信任 — 输入：T022、T030/T033（平台信任库） — 输出：`installCa` 先展示风险提示（本机 HTTPS 会被解密、仅限个人设备、可随时卸载），再写入系统信任库并回显状态 — 依赖：T022 — 验证：TC-E01（信任库可见本机 CA、UI 显示已安装、安装前有风险提示）；未装 CA 时开桥/访问 HTTPS 报 `CA_MISSING`（TC-E03） — 关联：REQ-010 / NFR-005 / AC-005 — 实现已完成（`platform/darwin/cert.ts`、`platform/win32/cert.ts` + 安装前风险提示；实机确认提示模态先于任何安装动作）；**当时未勾选**：TC-E01/TC-E02 需管理员密码写入系统信任库，本机无法提权，见 [verification.md](verification.md) 的「未验证 / 无法验证项」 ｜ **M4 结论**：TC-E03 通过、安装前风险提示通过；**自动**安装失败（osascript 提权子进程无法为信任设置弹授权，`KI-007`），TC-E01 改用应用自带的手动命令后通过 → 本任务保持未勾选，解除条件见 `KI-007` ｜ **`KI-007` 修复后复验（2026-09-21，[ADR-0008](../../docs/architecture/adr/ADR-0008-ca-trust-authorization-in-app-session.md)）**：安装改为两步（提权写系统钥匙串 + 应用进程写管理域信任设置）后，TC-E01 以**应用内自动路径**通过（全程不开终端）：`#message` = 「本机 CA 已安装并被系统信任。」、`#ca-state` = 「已安装并被系统信任」、`getCaStatus() = {installed:true,trusted:true}`、系统钥匙串含该证书、管理域信任项含该 CA 的 SHA-1、`verify-cert` 退出码 0、以该 CA 签发的叶证书经 `/usr/bin/curl` 返回 200；记录见 [verification.md](verification.md) 的「`KI-007` 修复复验执行记录（2026-09-21）」
- [x] T024 实现 Cert Manager：一键卸载 — 输入：T023 — 输出：`uninstallCa` 从系统信任库移除本机 CA，`getCaStatus` 回到未安装 — 依赖：T023 — 验证：TC-E02（信任移除、UI 状态同步） — 关联：REQ-010 / G-003 / AC-005 — 实现已完成（`uninstallCa` → `security delete-certificate -Z <sha1>` / `certutil -delstore`）；**M4 已勾选**：修复 `KI-010`（选择器补 `-Z`）后实机通过 TC-E02——`uninstallCa` 成功、系统钥匙串中 `mitmproxy` 证书 0 字节、UI 显示「未安装」、`getCaStatus() = {installed:false}`；残留观察（`verify-cert` 仍成功）记录在 `KI-010`
- [x] T025 实现 Allowlist/开关/状态 UI — 输入：T006、T015、T020 的 `getAllowlist` / `setAllowlist` IPC 与 App 数据目录下 `config.json` 的 allowlist 键 — 输出：主窗口状态条（未登录/已登录未开桥/桥接中/错误/过期处理中）、桥开关（未登录时禁用）、可增删主机的 allowlist 界面（输入主机名 + 「添加」、每行「删除」按钮、勾选 `启用 *.swufe.edu.cn`；修改立即生效，无需重启）、错误原因文案（`PROXY_CONFLICT` 提示先关闭 Clash / mihomo / sing-box 等） — 依赖：T006、T015、T020 — 验证：TC-B05 / TC-H02（界面添加主机与勾选通配后重启 App 仍在）、TC-H01（状态与状态机一致） — 关联：REQ-001 / REQ-005 / REQ-009 / AC-006 / R-004 — M3 证据：手工步骤——`pnpm start --user-data-dir=/tmp/m3-e2e` 启动后添加 `portal.swufe.edu.cn`（列表应出现该主机），退出并重启应用后读 App 数据目录的 `config.json` 确认仍在；输入非法值（如 `http://x/`）确认给出「主机名不合法」提示；点「删除」确认列表回落；勾选 `启用 *.swufe.edu.cn` 后重启确认仍为勾选；实机结果（2026-09-21，CDP 驱动真实应用）：添加/删除/非法值提示/通配勾选全部符合预期，重启后 `config.json` 仍为 `hosts=["jwxt.swufe.edu.cn"]` 且 `includeSwufeWildcard=true`
- [x] T026 实现捕获方式与进程捕获 UI — 输入：T007、T015、[docs/api/electron-ipc.md](../../docs/api/electron-ipc.md) 的 `listCaptureCandidates` / `setCaptureMode` / `setCaptureProcesses` — 输出：一级「捕获方式」单选（`system-proxy`（全部流量）/ `selected-apps`（指定应用））、候选应用列表（按应用名筛选、刷新列表、勾选；一个应用一行）、运行时配置 `bridge-config.json` 的 `capture.processes`、sidecar 的 `local:` 模式与 `swufe-capture` 回报（经 `BridgeStatus.captureError` 呈现失败，不进入桥 error 状态）；关闭开关或退出时一并停止捕获 — 依赖：T007、T015 — 验证：TC-G04（所选应用流量经桥、未被选中的应用不经桥；切回「系统代理」即停捕获） — 关联：REQ-003 / NFR-006 — 边界：与系统代理互斥——切到「指定应用」不设置系统代理并撤销本 App 设过的代理；切回「系统代理」恢复；切到「指定应用」前若系统代理被其它软件占用则以 `PROXY_CONFLICT` 拒绝且设置不落盘 — M3 证据：手工步骤——登录后选「指定应用」并勾选 `Google Chrome`（列表无该应用时用 pattern 为 `/usr/bin/curl` 的进程），首次授权需在 5 秒内确认；被选中进程访问本地上游应出现 `swufe-debug` 行、未选中进程不出现；切回「系统代理」应出现 `swufe-capture {"enabled": false, …}` 且 `networksetup -getwebproxy Wi-Fi` 恢复指向 `127.0.0.1:8080`；`bridges/python/tests/l0/test_capture.py`、`bridges/python/tests/l1/test_addon_capture.py`、`apps/desktop/test/orchestrator.test.ts`、`apps/desktop/test/store.test.ts` 覆盖模式互斥与 `capture.processes` 下发；实机结果（2026-09-21，CDP 驱动真实应用）：候选 363 行且 Chrome 主进程与 Helper 归并为一个 pattern、筛选与勾选生效并在重启后保留、切到「指定应用」时 `networksetup` 仍为 `Enabled: No`、系统代理被占用时切「指定应用」弹出冲突模态且 `captureMode` 不落盘；`swufe-capture` 行与模式集叠加/移除/回滚由 L0/L1 覆盖。**M3 未完成的部分已在 M4 补齐**：真实授权下的「只对所选应用生效」范围验证见下方 M4 证据 ｜ **M4 证据**：TC-G04 真实范围通过——切「指定应用」后系统代理 `Enabled: No`（互斥）、`localCaptureEnabled = true`、状态条「桥接中（进程捕获）」；被选中的 Chrome 经桥（桥日志 `jwxt.swufe.edu.cn rewritten=true` + `response body`），未选中的 `curl` 不经桥（`Could not resolve host` 且桥日志无对应行）；切回「系统代理」恢复 `127.0.0.1:8080`。本机未出现扩展授权提示（扩展此前已授权）
- [x] T027 实现调试日志面板 — 输入：T008、T012 的 `onDebugLog` 事件与 `setDebugLogging` 开关 — 输出：日志面板（三列：时间、域名、结果；最多保留最近 200 条；默认隐藏，随「调试日志」开关显示/隐藏；含「清空」按钮），内容仅域名与改写结果，不含正文、请求体与 Cookie — 依赖：T008、T012 — 验证：TC-F04 / AC-009（开启后产生流量逐条出现且无正文/Cookie；关闭开关后面板隐藏且记录清空） — 关联：REQ-009 / NFR-003 / AC-009 — M3 证据：手工步骤——开「调试日志」后面板出现，经桥访问一次 allowlist 主机后逐条出现时间/域名/结果三列且无正文与 Cookie；关开关后面板隐藏且记录清空；`apps/desktop/test/sidecar-lines.test.ts` 覆盖 `swufe-debug` 行只保留契约键（多余 `cookie`/`body` 字段被丢弃）；实机结果（2026-09-21，真实渲染层 + 真实 `onDebugLog` 推流）：面板三列表头、200 条上限且最新在前、清空、随开关显示/隐藏与关闭时清空、事件额外字段（cookie/body）不进入界面，全部符合预期
- [x] T028 实现 startBridge/stopBridge/getStatus 与桥状态机 — 输入：T015、T019、T020、T021、T022 — 输出：`BridgeStatus`（`state`/`loggedIn`/`systemProxyEnabled`/`localCaptureEnabled`/`bridgePort`/`error`）与开桥前置校验（会话、CA、allowlist 非空：`NOT_LOGGED_IN`/`CA_MISSING`/`ALLOWLIST_EMPTY`） — 依赖：T015、T019、T020、T021、T022 — 验证：TC-D02（未登录 `NOT_LOGGED_IN`）、allowlist 为空返回 `ALLOWLIST_EMPTY`（EC-006）、`getStatus` 各状态与实现一致（TC-H01） — 关联：REQ-001 / REQ-002 / REQ-010 / EC-006 — M2 证据：`apps/desktop/test/state-machine.test.ts` + `apps/desktop/test/orchestrator.test.ts`（前置校验收敛顺序、重复 start 幂等、失败回滚、过期级联）；实机状态条与状态机一致

## Phase 5 — 系统适配（WBS 5）

- [x] T029 实现 macOS 系统代理读写适配 — 输入：T020 — 输出：macOS 平台侧读取/设置/清除系统 HTTP/HTTPS 代理的实现（含「由本 App 设置」标记语义） — 依赖：T020 — 验证：macOS 上 TC-C02/C03/C04 通过 — 关联：REQ-003 / REQ-011 / NFR-006 — M2 证据：`apps/desktop/src/main/platform/darwin/system-proxy.ts` + `parse.ts` 单测（用本机实测的 `networksetup` 输出）；实机 TC-C02/C03/C04 通过
- [x] T030 实现 macOS 证书信任库适配 — 输入：T022 — 输出：macOS 侧 CA 写入/移除系统信任库的实现（需管理员权限） — 依赖：T022 — 验证：macOS 上 TC-E01/TC-E02 通过 — 关联：REQ-010 / REQ-011 — 实现已完成（osascript 提权的 `security add-trusted-cert` / `security delete-certificate`）；**当时未勾选**：TC-E01/TC-E02 未在真实信任库上验证（需管理员密码） ｜ **M4 结论**：卸载路径修复 `KI-010` 后实机通过（TC-E02）；**自动**安装路径失败（`KI-007`）→ 本任务保持未勾选，解除条件见 `KI-007` ｜ **`KI-007` 修复后复验（2026-09-21，[ADR-0008](../../docs/architecture/adr/ADR-0008-ca-trust-authorization-in-app-session.md)）**：`platform/darwin/cert.ts` 的安装改为两步——提权 `security add-certificates -k /Library/Keychains/System.keychain <caCert>`（仅钥匙串，重复导入的 `already in …` 视为成功）+ 应用进程执行 `security add-trusted-cert -d -r trustRoot <caCert>`（不带 `-k`，只写管理域信任设置）；卸载仍为提权 `security delete-certificate -Z <sha1>`。TC-E01/TC-E02 在 macOS 15.6 真机通过，`KI-007` 置 `Fixed`
- [x] T031 实现 macOS 权限引导与捕获切换文案 — 输入：T026 — 输出：两条文案 + 状态提示——(1) **进程捕获授权引导**：首次启用「指定应用」时 macOS 会安装并激活 mitmproxy 的网络扩展，需在「系统设置 → 通用 → 登录项与扩展」（或弹出的授权提示）中允许，且需在 5 秒内确认；超时则界面显示「启用失败」+ 该引导 + 「重试」按钮；(2) **切换捕获方式会撤销 / 恢复系统代理**：切到「指定应用」撤销本 App 设置的系统代理，切回「系统代理」恢复；授权缺失或捕获失败都不阻塞系统代理路径（桥仍 `running`，仅 `captureError` 就地显示） — 依赖：T026 — 验证：NFR-005 / TC-H01（手工步骤：拒绝或超时授权后界面给出可执行引导且系统代理路径仍可用；状态条文案与桥状态机一致，捕获生效时为「桥接中（进程捕获）」） — 关联：REQ-003 / NFR-005 / NFR-006 / R-004 — M3 证据：手工步骤——首次切到「指定应用」观察扩展授权提示，超时后确认出现「启用失败」+ 引导 + 「重试」；切到「指定应用」后 `networksetup -getwebproxy Wi-Fi` 为 `Enabled: No`，切回「系统代理」后恢复为 `127.0.0.1:8080`；捕获生效时状态条为「桥接中（进程捕获）」；实机结果（2026-09-21，CDP 驱动真实应用）：失败态显示「启用失败 — <原因>」+ 授权引导 + 「重试」，状态条回到「桥接中」（桥状态不变）；切到「指定应用」时系统代理未被设置。真实授权提示内的文案复验随 TC-G04 的真实范围验证
- [x] T032 实现 Windows 系统代理读写适配 — 输入：T020 — 输出：Windows 平台侧读取/设置/清除系统 HTTP/HTTPS 代理的实现 — 依赖：T020 — 验证：Windows 上 TC-C02/C03/C04 通过 — 关联：REQ-003 / REQ-011 / NFR-006 — 实现 + 单测通过（`parse.ts` 的 `REG_DWORD` / `REG_SZ` 与 per-protocol 解析、`Win32SystemProxy` 读写路径），真机验证延 M4/T038
- [x] T033 实现 Windows 证书存储适配 — 输入：T022 — 输出：Windows 侧 CA 写入/移除系统证书存储的实现 — 依赖：T022 — 验证：Windows 上 TC-E01/TC-E02 通过 — 关联：REQ-010 / REQ-011 — 实现 + 单测通过（`certutil` 命中判定与 `-user` 存储读写路径），真机验证延 M4/T038

## Phase 6 — 测试与验收（WBS 6）

- [x] T034 接入 L0 自动化测试（CI） — 输入：T004、T005、T006 — 输出：CI 每次 PR 运行 codec 向量与 allowlist 函数测试 — 依赖：T004、T005、T006 — 验证：CI 上 L0 全部通过（TC-A01..A05、TC-B01..B04） — 关联：NFR-002 — M1 证据：`.github/workflows/python-tests.yml`（CI 步骤在 `working-directory: bridges/python` 下运行；本地等价：`uv sync --frozen --directory bridges/python` + `uv run --directory bridges/python pytest tests/l0 -q`）
- [x] T035 编写 L1 组件测试（addon 对录制流量/假上游） — 输入：T008..T012 — 输出：本地 L1 测试集，覆盖请求改写决策与三类响应改写、Cookie 处理 — 依赖：T008..T012 — 验证：L1 全部通过（TC-F02、TC-F03） — 关联：REQ-006 / REQ-007 — M1 证据：`bridges/python/tests/l1/`（请求改写、响应三类反向改写、Cookie、日志最小化、热更新）
- [x] T036 编写 L2 集成测试（mitmdump + curl 经代理访问假 WebVPN） — 输入：T007、T008 — 输出：本地 L2 测试脚本，断言上游看到 WebVPN 形态与直连分流 — 依赖：T007、T008 — 验证：L2 通过（TC-F01） — 关联：REQ-006 / REQ-008 — M1 证据：`bridges/python/tests/l2/test_proxy_end_to_end.py`（真 mitmdump + curl + 假 WebVPN 上游）
- [x] T037 执行 macOS 教务手工验收 — 输入：T020..T028、真实账号与授权设备 — 输出：macOS 验收记录（TC-G01 打开教务首页、TC-G02 页面内导航不跳飞） — 依赖：T020..T028 — 验证：TC-G01、TC-G02 通过（不绑定 DOM，以关键路径可操作为准） — 关联：REQ-006 / REQ-007 / AC-007 / R-001 ｜ **M4 结果（2026-09-21）**：已实机执行但**未通过**——TC-G01 根 URL 白屏、TC-G02 页面不可交互（根因：网关客户端 shim 与透明桥不兼容，`KI-011`）；改写链路本身经 curl 与 Chrome 内 `fetch` 双向复核可用。**M5 结果（2026-09-21，`KI-011` 修复后复验）**：**通过**——入口 `http://jwxt.swufe.edu.cn/` 命中 `detail=promoted`、浏览器进入网关原生空间，教务首页（`xtgl/index_initMenu.html`，标题「教学管理信息服务平台」，菜单/用户信息完整）与站内「学生成绩查询」（`cjcx/cjcx_cxDgXscj.html`，查询表单 + 表格，无错误页）均可交互；`https://webvpn.swufe.edu.cn/wengine-vpn/js/main.js` 经桥 200 / 376922B；非 jwxt 主机 `www.swufe.edu.cn` 保持普通主机名且站内导航可用（`/xxgk.htm`）。命令与证据见 [verification.md](verification.md) 的 M5 记录
- [x] T047 复验 macOS 教务验收并采集 M5 证据（`KI-011` 修复后） — 输入：T046 的实现、真实账号与授权设备、`pnpm run acceptance:check` — 输出：TC-G01/TC-G02 的复验记录与脱敏证据（`specs/001-phase1-local-bridge/evidence/acceptance-macos/`） — 依赖：T046、T037 — 验证：真实应用 + 系统代理路径下，教务首页与站内导航可操作；桥日志出现 `detail=promoted`；`/wengine-vpn/js/main.js` 经桥 200；非 jwxt 主机仍在普通 URL 空间；期间顺带发现并登记 `KI-014`（CAS 主题资源被服务端截断） — 关联：REQ-007 / AC-007 ｜ **M5 证据**：见 [verification.md](verification.md) 的「M5（KI-011 修复）执行记录」与 `evidence/acceptance-macos/acceptance-darwin-20260921-154657.md`
- [ ] T038 执行 Windows 教务与进程捕获手工验收 — 输入：T032、T033、T026 — 输出：Windows 验收记录（TC-G03 教务首页；TC-G04 进程捕获） — 依赖：T032、T033、T026 — 验证：TC-G03、TC-G04 通过 — 关联：REQ-011 / AC-001 / AC-007 ｜ **M4 结论**：延期（Windows 测试机不在本轮可访问环境，`KI-001`）；Windows 步骤已写入 development-run.md 与 verification.md 的 M4 手册，解除条件见 `KI-001`
- [x] T039 缺陷修复与已知问题列表 — 输入：T034..T038 的执行记录 — 输出：P0 全部通过、P1 无未决阻断缺陷，已知问题列表（`id/title/severity/status/linked_case/owner/note`）已记录 — 依赖：T034..T038 — 验证：出口准则逐条核对（所有 P0 通过、P1 无未决阻断、教务验收至少一侧通过、已知问题已记录） — 关联：AC-001..AC-010 ｜ **M4 结果**：缺陷已收敛入 [known-issues.md](known-issues.md)——修复 `KI-008`（会话探测丢 Cookie）/`KI-009`（登录窗 ERR_ABORTED）/`KI-010`（CA 卸载缺 `-Z`）；`KI-002`/`KI-003` 转为 `Fixed`；未决 P0 `KI-011`（教务浏览器验收）+ P1 `KI-007`/`KI-013` + P2 `KI-006`/`KI-012`；Windows 延期 `KI-001`。"P0 全部通过"因 `KI-011` **未达成**，故 M4 保持 `In Progress` ｜ **M5 结果（2026-09-21）**：`KI-011` 转 `Fixed`（网关自有命名空间直通 + bootstrap 文档升级，见 [ADR-0007](../../docs/architecture/adr/ADR-0007-gateway-owned-namespaces-and-native-mode-promotion.md)），TC-G01/TC-G02 复验通过；仍未决：P1 `KI-007`（CA 自动安装）/ `KI-013`（TUN 干扰）/ `KI-014`（CAS 主题资源被服务端截断）、P2 `KI-006`/`KI-012`，Windows 仍延期（`KI-001`）
- [x] T043 编写 M4 双平台验收执行手册与结果表 — 输入：M4 里程碑退出条件、归档用例集 TC-A01..TC-H02、验证策略 — 输出：[verification.md](verification.md) 的「M4 双平台验收执行手册」小节与结果表 — 依赖：无 — 验证：手册步骤可被逐条执行且每步有期望与证据落点 — 关联：AC-001..AC-010 ｜ **M4 证据**：[verification.md](verification.md) 的「M4 双平台验收执行手册（交互式）」小节（步骤表含执行者/命令/期望/证据落点）+ 已逐行填写的 M4 结果表
- [x] T044 实现跨平台验收证据采集脚本 — 输入：Phase 2 需要的 OS 代理/信任库/CA 权限/桥存活/curl 对照证据 — 输出：`scripts/acceptance-collect.ts` 与 `pnpm run acceptance:check`，生成脱敏 markdown 报告 — 依赖：无 — 验证：桥关闭与桥运行两种状态下各跑一次，第二次 `bridge-port: open` 且 `bridge-smoke` PASS，报告内无任何 Cookie/密钥值 — 关联：NFR-003 / REQ-009 ｜ **M4 证据**：`scripts/acceptance-collect.ts` + `pnpm run acceptance:check`；桥关闭/桥运行两状态各跑一次（后者 `bridge-port: open`、`bridge-smoke: 200 OK`），哨兵值正负两向验证脱敏不变量（无泄漏 PASS、人为泄漏 FAIL + 退出码 1）；自检报告入库 `evidence/kit-selfcheck/`

## Phase 7 — 打包（WBS 7）

- [x] T040 编写开发版运行说明 — 输入：T014、T007 — 输出：开发运行说明（本机 Node + Python venv + mitmdump 的启动步骤与前置条件） — 依赖：T014、T007 — 验证：按说明在干净环境可启动开发版并通过 L0/L1 — 关联：NFR-006 ｜ **M4 证据**：[docs/operations/development-run.md](../../docs/operations/development-run.md)（+en）——前置条件（含 TUN 模式必须关闭）、首次准备、启动、管理员权限操作、验证与自检（指向 `pnpm run acceptance:check`）、常见故障、Windows 差异、停止与重置；README §3 与 apps/desktop/README 已交叉引用
- [ ] T041 构建安装包骨架（后置） — 输入：T040 — 输出：Electron 打包配置与 sidecar 外置的安装包构建脚本 — 依赖：T040 — 验证：构建产物可安装启动并开桥 — 关联：REQ-011 / R-003
- [ ] T042 确定 sidecar 分发形态并验证体积 — 输入：T041、[spec.md](spec.md) Q-002 / DQ-002 — 输出：嵌入式 Python 与外置 `mitmproxy` 可执行文件的取舍结论与体积记录 — 依赖：T041 — 验证：记录两种形态的包体积与启动验证结果；跨平台分发说明完整 — 关联：REQ-011 / NFR-001 / R-003

## 注意事项

- 同一文件的任务应串行，或先指定集成责任人；
- 需要外部决策的任务先标记阻塞，不要用猜测推进；
- 任务完成但验证未通过时，保持未勾选，并在 [verification.md](verification.md) 中记录 `Failed`；
- 发现新任务时追加到对应 Phase，不要扩大既有任务范围。
